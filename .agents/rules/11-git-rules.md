# Git Rules

- **Diffs:** Inspect `git diff` and `git status` before committing.
- **Commits:** Write clear, meaningful commit messages.
- **No Force Pushing:** Never force push over user work.
- **No Secrets:** Never commit `.env` or other sensitive files.
