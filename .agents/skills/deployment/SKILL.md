---
name: deployment
description: "Deployment workflows for the project."
---

# Deployment

*(Note: The project currently does not have a defined CI/CD pipeline in its repository. This skill will be expanded once deployment infrastructure (e.g., Vercel, AWS) is set up.)*

Always ensure `npm run build` passes before any deployment.
